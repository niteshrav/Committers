/** Must match frontend `BUDGET_RANGE_OPTIONS` (excluding empty). */
export const LEAD_BUDGET_RANGE_VALUES = [
  "Under $1,000",
  "$1,000 – $5,000",
  "$5,000 – $15,000",
  "$15,000 – $50,000",
  "$50,000+",
  "Not sure yet",
] as const;
